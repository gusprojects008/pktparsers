Atualmente estou começando a desenvolver o pktparsers, o módulo de parsers de frames, pacote e campos de rede, de diferentes padrões de comunicação diferentes. Ele será utilizado por programas como o framesniff, e por outros módulos como o wnlpy (Wrapper/Wireless Netlink Python) para parsear os blobs retornados por uma mensagem nl80211 de operação de scan. 
Logo logo vou adaptar o framesniff para utilizar ele.

O que básicamente estou fazendo agora, é mover toda a estrutura de parse criada para o framesniff, para "core" de pktparsers. Para assim, torna-lo reutilizavel em outros projetos.

Mas estou começando a pensar em como posso implementar algumas funcionalidades que vou utilizar no meu framesniff, essas funcionalidades dependem diretamente da funcionalidade/capacidade de parse que o módulo pktparsers oferece.

Percebi que tenho que tomar algumas decisões arquiteturais, pois notei que todo o meu parse é idealmente voltado para a ideia que tive no começo do meu projeto framenisff, mas que estou percebendo que provavelmente irei ter que mudar. A ideia era de que todo o meu parse e o próprio framesniff irá funcionar apenas para a captura e analise do trafégo de rede e partir da camada 2 (Data Link) em diante (teoricamente descriptografando os payloads sub-sequentes).
Mas percebi que como agora estou modularizando o sistema de parse, movendo ele para um módulo separado (pktparsers), então meus parsers não devem depender desse padrão de parse, ou seja, do padrão baseado na ideia de receber um frame da menor camada, e ir parseando ele e os seus payloads em diante.

Acredito que a melhor solução seja justamente seguir com a ideia do registry, registrar parse de acordo com a DLT (Data Link Type) específica, pois assim, poderei por exemplo, em frames DLT_IEEE802_11_RADIO, obter parsers para interpretar payloads LLC da camada 3, 4, através de uma função como get_parser passando a DLT_EN10MB com string ou como valor.

Atualmente minha ideia com esse módulo "pktparsers" é:
Um programa como framenisff poderá criar uma função de sniff que abre um socket de captura de frames DLT_IEEE802_11_RADIO, e assim pode utilizar pktparsers para parsear cada frame capturado.
Mas outros módulos como wnlpy poderão acessar acessar funções de módulos específicos de parse do pktparsers, como as funções de parse ie ou tagged_parameters.

Quero implementar:
Traffic Summary, semelhante à WLAN Traffic no wireshark, mas com diferenças.

Estrutura que o wireshark fornece:

===============================================================================================================================================
Wireless LAN Statistics - wlp0s20f3:
Address  Channel  SSID  Percent Packets  Percent Retry  Retry  Pkts Sent  Pkts Received  Probe Reqs  Probe Resp  Auths  Deauths  Other  Comment
-----------------------------------------------------------------------------------------------------------------------------------------------
28:87:2e:9f:f6:85        0  <Broadcast>         0.061805       0.000000      0          0              1           0           0      0        0      0  Unknown
28:87:ba:9f:f6:85        1  404wi-fi indisponível        98.269468       0.125786      2       1360            188           0          42      0        0      0  Unknown
48:22:54:7e:69:48        1  Gisele Matos         0.185414       0.000000      0          3              0           0           0      0        0      0         
4a:22:54:4e:69:48        1  <Broadcast>         0.432633       0.000000      0          7              0           0           0      0        0      0         
4a:22:54:5e:69:48        1  <Broadcast>         0.309023       0.000000      0          5              0           0           0      0        0      0         
80:1f:65:5a:67:95        0  <Broadcast>         0.061805       0.000000      0          0              1           0           0      0        0      0  Unknown
ee:81:9c:85:b0:dc        0  <Broadcast>         0.185414       0.000000      0          0              3           0           0      0        0      0  Unknown
ee:81:9c:85:b0:df        0  <Broadcast>         0.309023       0.000000      0          0              5           0           0      0        0      0  Unknown
ff:ff:ff:ff:ff:ff        1  <Broadcast>         0.185414       0.000000      0          0              0           3           0      0        0      0         
-----------------------------------------------------------------------------------------------------------------------------------------------

O que estou desenvolvendo para poder implementar o traffic summary:
Como traffic summary irá ser:
TrafficSummary terá vários DeviceEntry, que provavelmente estarão dentro de um set:
{DeviceEntry}, {DeviceEntry}, {DeviceEntry} ...
Pois poderá haver um dispositivo com o mesmo MAC na rede, podendo assim, sobrescrever ou gerar conflito no momento da criação de novos DeviceEntry(s).
Mas visualmente:
Traffic Summary - wlp0s20f3:
"ip, mac address, ou qualquer outro endereço principal relacionado ao dispositivo de acordo com a camada/padrão de comunicação principal sendo capturado através da interface, ou seja, mac address se o sniff for a partir da camada L2, ip se o sniff for a partir da camada L3 ou L4": DeviceEntry

Tecnicamente:



DeviceEntry: ainda estou em dúvida sobre como irei estruturar ele, pois não sei se crio uma estrutura DeviceEntry universal, que irá estruturar todos os dados relevantes através de qualquer função utilizada em TrafficContext para criar DeviceEntry através das estruturas de Summary que ela está preparada para lidar dentro ou fora do frame parseado.
Ou seja, TrafficSummary irá conter várias entradas DeviceEntry, visualmente seria:

A questão é que agora estou pensando em implementar:
As estruturas de Summary que acho que vou ter que implementar (através de dataclass):
FrameSummary (l2)
PacketSummary (l3)
SegmentSummary (l4)
DataSummary (l5, l6, l7)
TrafficSummary

Provavelmente irei criar um diretório "summaries" para agrupar os módulos que irão definir essas estruturas de Summary, mas acho que o correto vai ser criar o diretório "summaries" dentro de cada padrão de comunicação relacionado a uma DLT de captura possível. Ou seja, irei criar o diretório "summaries" ou "summary" dentro de ieee802/dot11/ pois dot11 é o padrão de comunicação relacionado a DLT de captura "DLT_IEEE802_11_RADIO".



Pensei no fluxo:
Função sniff de framesniff (por exemplo), ou a classe Dissector ou função dissector de pktparsers, cria TrafficContext.
TrafficContext usa uma função que consome FrameSummar para criar TrafficSummary.
TrafficContext consome FrameSummary para criar DeviceEntry.
FrameSummary consome descriptions do resultado do frame ou pacote parseado.

Mas ainda tenho muitas dúvidas em relação à forma como irei gerar as estruturas de Summary:

Dúvidas em relação a FrameSummary:
E se ao invés de gerar os descriptions durante o parse de campos específicos, para que uma função de Frame ou Packet summary obter eles e gerar as estruturas de summary. Na verdade ter apenas uma função para analisar todo o frame ou pacote parseado e gerar todas as estruturas de Summary?
A grande é questão é que por exemplo, frames contém payloads, e esse próprios payloads são pacotes que, podem conter segmentos que, podem conter dados. Dessa forma, irei ter uma função única para percorrer todo o frame parseado e gerar esse summaries de acordo com uma analise lógica e conceitual da estrutura do frame ou pacote? ou chamar a função summarie específica dentro do parse da camada específica? exemplo: parse de pacote ARP chama função de summarie de pacote, dessa forma, a estrutura summarie fica disponível diretamente no resultaod final do frame parseado.
Se for função única, onde ela ficaria? no diretório relacionado ao padrão específico? ou no diretório relacionado à camada de rede?

Dúvidas em relação à TrafficContext:
A função que irá analisar o frame parseado com as estruturas de Summary, irá ser provavelmente chamada dentro de ParseContext, e ela, irá gerenciar a variável de traffic_context do contexto de TrafficContext criado por uma função ou classe superior.
A estrutura TrafficSummary vai se global? ou de acordo com a camada ou padrão de comunicação específico?

Será que pktparsers poderá/irá ter funcionar assim?:
Dissector -> TrafficContext -> ParseContext
Dessa forma, uma função como sniff do framesniff, irá importar a classe ou função dissector de pktparsers, a função dissector teria a assinatura:
(dlt: str, packet: bytes, offset: int = 0) "packet" pois dissector não sabe realmente se é frame, pacote, ou segmento, apenas irá chamar o parse completo de acordo com a DLT passada.

A questão é que quero seguir um mesmo padrão de estrutura de parse em cada padrão de comunicação ou camada diferente, tenho que decidir se haverá uma função de parse principal em cada diretório relacionado a um padrão de comunicação específico, ou de acordo com camada de rede.
ParseContext gera os Summaries através de uma função específica relacionada ao seu padrão de comunicação ou camada na rede, que irá analisar todos os descriptions do frame parseado, ou todo o frame parseado em si (ainda tenho que decidir como vou gerar os Summary de cada frame, pacote, segmento ou dado específico). ParseContext provavelmente irá ter que chamar a funçao responsável por gerenciar a variável de traffic_context, analisando os summaries gerados, criando DeviceEntry de acordo com eles, e populando o traffic_context. Essa função responsável por gerenciar TrafficContext vai ser chamada de acordo com o padrão de comunicação ou camada de rede específica.

Acho que necessário revisarmos o principal objetivo com a ideia de implementar:
Descrição de conteúdo parseado.
Resumo do frame.
Resumo de tráfego. 

Os principais objetivos são:
Poder criar uma estrutura DeviceEntry que conterá vários dados sobre um dispositivo com base nos frames e pacotes capturados relacionados ao endereço dele.
Facilitar módulos como scan_monitor.py obterem informações relevantes para exibir na interface TUI. Pois atualmente ele precisa navegar sobre todo o frame parseado e obter cada dado parseado, para interpretar e assim chegar à conclusão de algumas informações. Assim como as funções das classe Hashcat, usadas para gerar linhas utilizadas para brute force pelo hashcat, seria muito mais fácil se houve um description do conteúdo parseado do payload EAPOL, indicando se é frame 1, 2, 3 ou 4. Ou ao invés disso, ter essa informação diretamente em FrameSummary.


As estruturas que andei pensando:
Padrão de summary.py para cada DLT de captura:
# pktparsers/layers/l2/ieee802/dot11/summary/summary.py

from dataclasses import dataclass, field
from typing import Optional

@dataclass
class FrameSummary:
    frame_type: int       # "Management", "Control", "Data"
    frame_subtype: int          # "Beacon", "Probe Request", etc
    channel: int = None
    ssid: str = None
    bssid: dict = None
    sa: dict = None
    da: dict = None
    signal_dbm: Optional[int] = None
    wps: dict = None
    eapol: dict = None  # {message: 1|2|3|4, flags: {}}
    
def summarize_dot11(parsed_frame: dict) -> FrameSummary:
    rt_hdr = parsed_frame.get("rt_hdr", {}).get("parsed", {})
    mac_hdr = parsed_frame.get("mac_hdr", {}).get("parsed", {})
    body = parsed_frame.get("body", {})
    
    fc = mac_hdr.get("fc", {})
    channel = rt_hdr.get("channel")
    
    eapol_summary = None
    llc = body.get("llc", {})
    if isinstance(llc, dict):
        payload = llc.get("payload", {})
        if isinstance(payload, dict) and "parsed" in payload:
            eapol_summary = _summarize_eapol(payload["parsed"])
    
    return FrameSummary(
        frame_type=fc.get("frame_type"),
        subtype=fc.get("frame_subtype"),
        channel=channel.get("channel"),
        signal_dbm=rt_hdr.get("dbm_antenna_signal"),
        bssid=mac_hdr.get("bssid"),
        sa=mac_hdr.get("sa"),
        da=mac_hdr.get("da"),
        ssid=body,
        eapol=eapol_summary,
    )

def _summarize_eapol(eapol: dict) -> dict:
    ki = eapol.get("key_information", {})
    ack = ki.get("key_ack", False)
    mic = ki.get("key_mic", False)
    inst = ki.get("install", False)
    sec = ki.get("secure", False)
    
    if ack and not mic and not sec:
        msg = 1
    elif not ack and mic and not sec:
        msg = 2
    elif ack and mic and inst:
        msg = 3
    elif not ack and mic and sec:
        msg = 4
    else:
        msg = 0
    
    flags = []
    if msg in (1, 2):
        flags.append("HANDSHAKE_CAPTURABLE")
    if not mic and msg == 1:
        flags.append("NO_MIC")
    
    return {"message": msg, "flags": flags}


Ou seja, a função summarize_dot11 seria responsável não apenas por analisar lógicamente (de acordo com o protocolo e padrão de comunicação) o resultado do frame parseado, 

A função responsável por criar ou atualizar uma entrada DeviceEntry(s) específica em traffic_summary, a partir das estruturas de Summary obtidas pela função de traffic context. A questão é como essa função de traffic context irá obter essas estruturas de Summary, formas que pensei:
forma 1:
Percorrer todo frame parseado de forma lógica, proucurando estruturas de Summary, obter elas, centralizar elas temporariamente, e começar a obter os dados relevantes para criar ou atualizar os DeviceEntry(s) em traffic_summary.

forma 2:
Adaptar as funções de summary ou, de alguma outra forma, centralizar todas as estruturas de Summary em um único dicionário de uma forma lógica e conceitualmente correta. Facilitando assim, a função de traffic context, obter as estruturas de Summary e então extrair os dados relevantes e necessários para criar ou atualizar entrada de DeviceEntry em traffic_summary.
Provavelmente ficando em alguma dessas formas:
forma 1:
summaries = {
    "frame": {
        "DLT_IEEE802_11_RADIO": FrameSummary,
        "DLT_EN10MB": FrameSummary,
    },
    "packet": {
        "arp": PacketSummary,
        "ip": PacketSummary,
    },
    "segment": {
        "tcp": SegmentSummary,
        "udp": SegmentSummary
    },
    "data": {
        "http": DataSummary,
    }
}

Observação: no caso da parte de "segment", eu pensei sobre os termos técnicos específicos como "datagram" ou "message" mas não sei como poderia utilizar eles, pois eles não parecem ser realmente necessários nesse caso.

em busca de cada estrutura de Summary gerado por parser de cada DLT ou camada diferente? exemplo:
Função de parse da DLT_IEEE802_11_RADIO cria FrameSummary, função de parse da DLT_EN10MB cria FrameSummary, função de parse da camada 3 cria PacketSummary, função de parse da camada 4 cria SegmentSummary, e função de parse da camada 5 em diante criar DataSummary para cada payload carregado.

Esse é só um exemplo, pois existem alguns problemas:
A função summarize_dot11 assume que existe dicionários como "eapol" sem verificar os dados do conteúdo parseado, que indicam a presença de certos dicionários como "eapol", essa é a questão, a função de "summarize" do parse principal de alguma DLT como: DLT_IEEE802_11_RADIO, deve ser responsável por percorrer o conteúdo do frame parseado de forma lógica de acordo com o protocolo, e buscar dicionários como "eapol" de acordo com os dados do resultado do frame parseado, como frame_type e frame_subtype.

Essas funções não estou utilizando a função auxiliar "get_nested".

Por que ainda estou pensando/considerando a ideia de extender/atualizar a função unpack para poder receber uma callback chamada "descriptor" que será usada para criar um dicionário com a descrição do conteúdo parseado pela callback "parser" de unpack, ainda teria que ver melhor a forma como isso iria ser feito, mas o importante seria a ideia de gerar o dicionário "description" para facilitar o trabalho da função de summarize da DLT específica, que irá apenas obter o description para atribuir à variável específica dentro do dataclass de Summary.

DeviceEntry:
from dataclasses import dataclass, field
from collections import defaultdict

@dataclass  
class DeviceEntry:
    addr: str
    layers: dict = {"l2": {"channel": , "ssid": , "percent_frames": , "percent_retry": , "retry": , "frames_sent"  Pkts Received  Probe Reqs  Probe Resp  Auths  Deauths  Other  Comment, "channels_seen": = field(default_factory=set), "vendor": None, 
    role: str = "unknown"          # "ap", "sta", "unknown"
    ssid: str | None = None        # se for AP
    associated_bssid: str | None = None  # se for STA
    relationships: dict[str, dict] = {frames}
    security: dict = None 
    last_seen: float = 0.0
    sessions: list = field(default_factory=list)
   
Em "layers: dict = {"l2": {}}" acredito que não necessáriamente "l2" precise ser um dicionário, acho que seria melhor se fosse um dataclass com o FrameSummary.

Dando contexto maior sobre o problema:

Atualmente o repo de pktparsers:


Atualmente framesniff:


Atualmente wnlpy:





Acredito que para cada função de parse relacionada a uma DLT específica, vou ter que provavelmente possibilitar o usuário passar um argumento com o dicionário com as chaves de descriptografia de payloads das camadas subsequentes, só não sei bem como vai ser a estrutura desse dicionário, pensei em algumas formas:

forma 1 (por camada de rede):
{
  "l2": {
    "bssid": {
      "psk": ,
      "ssid": ,
      "clients": {
        "MAC do CLIENTE": {
          "handshake (opcional)": [lista frames de handshake relacionado ao endereço mac cliente],
          "pmk
        },
      }
    }
  }
}

Mas ainda tem alguns problemas:
E se for WEP, WPA3 ou WPA enterprise?
Como funcionaria nas camadas l3+ ? provavelmente da mesma forma mas, de acordo com a estrutura de pacote DLT_EN10MB
